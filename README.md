
# Jekyll Portfolio Starter (Minimal Mistakes)

1. Replace `USERNAME` in `_config.yml` with your GitHub username.
2. Commit and push to `main` in `USERNAME.github.io`.
3. Enable GitHub Pages from Settings → Pages (Source: `main`).
